---
name: xhs-content-pipeline
description: |
  小红书内容自动化流水线。从选题推送、选题分析、文案生成、配图生成到飞书归档，全程自动执行。
  当用户在群里说"推选题"、"开始选题"、"小红书流程"，或回复选题编号（1/2/3）时触发。
version: 1.0.0
---

# 小红书内容自动化流水线 (XHS Content Pipeline)

## ⚠️⚠️⚠️ 核心规则（最高优先级，必须严格遵守）⚠️⚠️⚠️

1. **所有群聊消息必须用 `message` 工具发送**（机器人身份），禁止用 `feishu_im_user_message`（用户身份）
2. **每个步骤必须全部完成才能停止**，不能中途停下来说"等XX来做"或"下一步等你确认"
3. **自动继续**：群里有人回复了就继续往下走，不要停在那等DM对话里回复
4. **完整执行6步流程**，缺一步都不算完成
5. **⚠️ 选题必须从选题库(tblBBoGrB5W6DfB4)取，不是选题分析(tblQ8BCiybOzQa2z)！**
6. **⚠️ 配图必须自己生成，不能说"等XX来配图"！调用图片生成工具自己完成！**

---

## 触发条件

- 群聊中有人 @Alina 说"推选题"、"开始选题"、"小红书流程"
- Cron 定时任务触发（每天早上8点）
- 用户在DM里说"推选题到群"

---

## 完整流程（6步，必须全部执行）

```
第1步：推选题 → 从选题库筛选3个 → 推送到飞书群
第2步：选题分析 → 用户选号后 → 分析结果 → 推送到飞书群
第3步：生成文案 → 小红书文案 → 推送到飞书群
第4步：生成配图 → 生成图片 → 上传飞书 → 推送链接到飞书群
第5步：写入表格 → 小红书内容生成库
第6步：推送完整草稿 → 可直接复制发布 → 推送到飞书群
```

**⚠️ 每一步完成后自动进入下一步，不要停下等待！**

---

### 第1步：推送选题（⚠️ 从选题库取，不是选题分析！）

1. **⚠️ 必须从【选题库】取数据！不是选题分析！**
   - app_token: NdpBbD8jray5fDs77gScXZ9vnId
   - **选题库 table_id: tblBBoGrB5W6DfB4** ← 从这里取！
   - 选题分析 table_id: tblQ8BCiybOzQa2z ← 这个是第2步写入用的，不是取数据用的！
2. **⚠️ 筛选条件（必须满足）**：
   - 只选**今天采集**的数据（采集时间 = 今天）
   - 在今天数据中按**推荐度降序**排列
   - 优先选**发布时间最近**的
   - 如果今天没有采集数据，告知群里"今天没有新采集的选题"，并从库里选最近采集的
3. 取前3个，用 `message` 工具推送到飞书群
4. 等待用户在群里回复编号（1/2/3）
5. 用户回复后，**立即进入第2步**

### 第2步：选题分析

1. 根据用户选的编号确定选题
2. 对选题进行深入分析（目标人群、爆款方向、创作建议）
3. 写入【选题分析】表(tblQ8BCiybOzQa2z)（如果表格字段不匹配，跳过写表，直接推送分析）
4. 用 `message` 工具推送分析结果到飞书群
5. 等待用户回复方向（如"方向1"、"2"）
6. 用户回复后，**立即进入第3步**

### 第3步：生成文案

1. 根据确认的方向生成小红书文案（标题、正文、标签）
2. 用 `message` 工具推送到飞书群
3. **⚠️ 不要说"等配图"，直接进入第4步**

### 第4步：生成配图

1. 根据文案内容生成图片提示词
2. 调用图片生成工具（scripts/xhs_image_gen.py 或 qwen-image 相关工具）
3. 如果图片生成失败，推送文案时说明"配图请手动补充"，然后继续第5步
4. 如果成功，按实际数量命名（P1、P2、P3、P4）
5. 上传到飞书：
   - `feishu_drive_file` action=upload（上传到根目录）
   - `feishu_drive_file` list 获取文件token
   - `feishu_drive_file` action=move 移动到目标文件夹(D82efvisxlshojd9qTscNkxrnEh)
6. 用 `message` 工具推送图片链接到飞书群
7. **⚠️ 图片推送完后，立即进入第5步**

### 第5步：写入小红书内容生成库

1. 写入【自媒体数据采集中心v2】→【小红书内容生成库】(tblcdl9yD51wZHVt)
2. 字段格式：
   - 选题、标题、正文内容、标签、发布状态=待发布
   - 配图链接1-5（URL字段格式：`{"link": "url", "text": "描述", "type": "url"}`）
3. **⚠️ 写入后，立即进入第6步**

### 第6步：推送完整草稿到飞书群（可直接发布）

1. 用 `message` 工具推送完整草稿，格式：

```
📝 小红书完整草稿（可直接发布）

标题：xxx

正文：
（完整正文，保留格式、emoji、段落分隔）

标签：#标签1 #标签2 #标签3

配图：
P1: https://...
P2: https://...
P3: https://...
P4: https://...

📊 表格链接：https://my.feishu.cn/base/...
```

**⚠️ 这是最后一步，完成后流程结束**

---

## 关键规则

### 消息身份
- **所有群聊消息用 `message` 工具（channel=feishu, target=chat:oc_602f35b88d99d65d32d21b4510260e0f）**
- 禁止用 `feishu_im_user_message`

### 自动继续
- **群里有人回复了就继续往下走**
- 用 `feishu_im_user_get_messages` 检查群消息
- 不要停在那等用户在DM里回复

### 飞书表格
- URL字段必须用 `{link, text, type}` 格式
- 正文内容中不要包含英文引号 `"`（会导致字段解析失败）

### 飞书上传
- folder_token 参数不生效，先上传根目录再move
- 目标文件夹 token：`D82efvisxlshojd9qTscNkxrnEh`

---

## 表格ID速查

| 表格 | app_token | table_id |
|------|-----------|----------|
| 选题库 | NdpBbD8jray5fDs77gScXZ9vnId | tblBBoGrB5W6DfB4 |
| 选题分析 | NdpBbD8jray5fDs77gScXZ9vnId | tblQ8BCiybOzQa2z |
| 小红书内容生成库 | NdpBbD8jray5fDs77gScXZ9vnId | tblcdl9yD51wZHVt |

## 飞书资源

| 资源 | ID/Token |
|------|----------|
| 飞书群 | oc_602f35b88d99d65d32d21b4510260e0f |
| 目标文件夹 | D82efvisxlshojd9qTscNkxrnEh |
| 飞书表格 | NdpBbD8jray5fDs77gScXZ9vnId |

---

## 品牌风格参考

色调：深青绿、青灰绿、暖米色、柔和金黄
质感：纸张纹理、手绘感、水彩晕染
风格：治愈系、温暖、自然意象、正念
元素：山川、河流、月亮、花草、自然物
字体：手写感中文、细体英文辅助
背景：浅青绿色或米白
